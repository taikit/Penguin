var React = require('react');

var Rooms = React.createClass({
    render: function () {
        return (
            <div>
                this is rooms
            </div>
        );
    }
});

module.exports = Rooms;
